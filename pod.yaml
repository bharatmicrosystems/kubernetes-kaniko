apiVersion: v1
kind: Pod
metadata:
  name: kaniko
spec:
  containers:
  - name: kaniko
    image: gcr.io/kaniko-project/executor:latest
    args: ["--dockerfile=/workspace/Dockerfile",
            "--context=dir://workspace",
            "--destination=<user-name>/<repo>"] # replace with your dockerhub account
    volumeMounts:
      - name: kaniko-secret
        mountPath: "/kaniko/.docker"
      - name: dockerfile-storage
        mountPath: "/workspace"
  restartPolicy: Never
  initContainers:
  - name: init-kaniko
    image: yauritux/busybox-curl
    imagePullPolicy: IfNotPresent
    command: ["sh", "-c", "curl -o /workspace/Dockerfile URL"]
    volumeMounts:
    - mountPath: "/workspace"
      name: dockerfile-storage
  volumes:
    - name: kaniko-secret
      secret:
        secretName: regcred
        items:
          - key: .dockerconfigjson
            path: config.json
    - name: dockerfile-storage
      emptyDir: {}
